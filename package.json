{
  "name": "MovieCatalog",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://github.com/Mudreuski/WebTest",
  "devDependencies": {
    "gh-pages": "^1.1.0",
    "react-scripts": "^5.0.1"
  },
  "dependencies": {
    "@appbaseio/reactivesearch": "^2.0.0-beta",
    "react": "^16.2.0",
    "react-dom": "^16.2.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  }
}
